mport matplotlib.pyplot as plt
import random

x = list(range(10))
y = [random.randint(0,10) for _ in x]

plt.bar(x,y); plt.title("Bar chart"); plt.show()
plt.plot(x,y); plt.title("Line chart"); plt.show()
